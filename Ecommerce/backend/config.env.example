# Database Configuration
DATABASE_URL=postgres://appuser:apppassword@localhost:5432/ecommerce

# JWT Configuration
JWT_SECRET=un-secreto-muy-seguro-y-largo

# Server Port
PORT=8080

# WebAuthn (Passkeys) Configuration
# RPID (Relying Party ID) es el dominio de tu aplicación. Para desarrollo local, es 'localhost'.
WEBAUTHN_RPID=localhost
# RPOrigins son las URLs desde las que se permitirán los registros/inicios de sesión.
WEBAUTHN_RP_ORIGIN=http://localhost:3000

# Production WebAuthn Configuration (para axiora.pro)
# WEBAUTHN_RPID=axiora.pro
# WEBAUTHN_RP_ORIGIN=https://axiora.pro

# Stripe Configuration
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Mailer/Resend Configuration
EMAIL_FROM="Tu Tienda <noreply@localhost>"
# RESEND_API_KEY=tu-api-key